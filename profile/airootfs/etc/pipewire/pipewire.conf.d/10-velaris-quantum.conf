# Velaris — equilíbrio entre baixa latência e estabilidade em hardware modesto
context.properties = {
    default.clock.rate          = 48000
    default.clock.quantum       = 512
    default.clock.min-quantum   = 64
    default.clock.max-quantum   = 8192
}
